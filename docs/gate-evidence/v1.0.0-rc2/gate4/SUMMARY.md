# Gate 4 evidence — v1.0.0-rc2

Generated by `go run ./tools/gate4-traceability`. Do not edit by hand: every
status below is computed from the dispatch tables, the route classification and
the parsed specification, so editing this file changes the record without
changing the fact.

## 4.1 — capability rows

| Status | Rows |
| :-- | --: |
| verified | 58 |
| unreachable | 0 |
| deferred | 0 |
| **total** | **58** |

Gate 4.1 requires 58 rows, all verified. **MET.**

## 4.2 — ESI route coverage

| Disposition | Routes |
| :-- | --: |
| mapped to a live subscription | 102 |
| deliberately unmapped — fetched-by-parent | 1 |
| deliberately unmapped — no-capability | 26 |
| deliberately unmapped — notification-sourced | 3 |
| deliberately unmapped — on-demand | 1 |
| deliberately unmapped — post-v1.0 | 26 |
| deliberately unmapped — sde | 31 |
| **total catalogued GET routes** | **190** |

Gate 4.2 requires every measured route mapped or recorded with a reason. Every
route above carries one, so the partition is total — but `not-built` is a
RECORDED FAILURE and not an exemption: those routes back an Appendix A
capability whose table, store queries and API endpoint exist while no sync
handler writes it, so the endpoint serves an empty result forever.

## Measured baseline (docs/BASELINE.md)

| Dimension | Measured |
| :-- | --: |
| distinct ESI routes | 106 |
| UI controllers | 72 |
| alert types | 54 |
| UI locales | 9 |
| /api/v2 controllers | 9 |

## Gate 7 — /api/v2 shim coverage

| Status | Routes |
| :-- | --: |
| served | 16 |
| pending | 12 |
| unshimmable | 4 |
| breaking | 2 |
| **total** | **34** |
