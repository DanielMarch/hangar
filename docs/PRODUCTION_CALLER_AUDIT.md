# Production-caller audit — Phase 20

**Question asked of every subsystem:** *does any non-test file, outside this package, actually
call this code on a path reachable from `main`?*

This is the audit SRS §0's B20/B22/B23 entries describe, run exhaustively rather than by
inspection. It is a **Gate 4 prerequisite**: Gate 4.1 requires 58 capability rows all at
`status = verified`, and a capability whose implementation is unreachable from `cmd/hangar` is
not delivered, however green its own package tests are.

---

## Method

Two passes, both mechanical and both reproducible.

### Pass 1 — package reachability

```bash
comm -23 <(go list ./... | sort) <(go list -deps ./cmd/hangar | grep hangar-project | sort)
```

A package absent from the binary's transitive imports cannot execute in production under any
input. This is the strongest form of the finding and admits no false positives.

### Pass 2 — function reachability (RTA)

```bash
go run golang.org/x/tools/cmd/deadcode@latest -test=false ./cmd/hangar
```

Rapid Type Analysis over the call graph rooted at `main`. Unlike a grep, it is not fooled by a
symbol that appears only inside a doc comment — which is exactly how B22 and B23 hid, and how
every finding below hid.

Every finding in the tables below was then **confirmed by hand**: grepping the symbol across all
non-test `.go` files and checking each hit is a real call rather than prose. Findings where the
only hits were comments are marked *comment-only*, which is the signature of this defect class.

### Reproduction

```bash
go run golang.org/x/tools/cmd/deadcode@latest -test=false ./cmd/hangar
```

124 unreachable functions at commit `7a74e5d`.

---

## Pass 1 result — packages with no production caller at all

| Package | Status |
| :-- | :-- |
| `internal/sde` | **B22**, already recorded. Whole import pipeline. |
| `internal/i18n` | **B23**, already recorded. Not merely `ResolveESILanguage` — the *package* is absent from the binary, so `UILocales()` (the closed locale set the SRS says Phase 15 middleware validates against) has no caller either. |
| `internal/esi/pagination` | **NEW — B31.** See below. |
| `tools/gen-permission-seed` | Not a defect: a standalone generator `main`, correctly excluded. |

---

## Pass 2 result — subsystems built, tested, and unreachable

Grouped by the gate each one blocks. "T" means the symbol has passing tests and no production
caller — the exact B20 pattern.

### Blocking Gate 1 (ESI Load Stability)

| Defect | Evidence |
| :-- | :-- |
| **B29 — ESI rate-limit header reconciliation never runs.** `ratelimit.ClassifyResponse`, `ParseRemaining`, `ParseRateLimitLimit` are all unreachable [T]. `esi.Client.Do` calls only `ratelimit.ClassifyCost`. | `ClassifyResponse` appears in non-test source exactly once, in a doc comment on `esi.Client.TTLFloor` describing behaviour that does not happen. Gate 1.3 (divergence ≤ 1), Gate 1.4 (proactive pause), and the `429`-headerless / `Retry-After` / server-reports-lower / server-reports-higher adversarial rows in §1.3 have no live implementation to exercise. |
| **B28 — the entity circuit breaker never runs.** `breaker.NewEntityBreaker`, `EntityBreaker.{Allow,RecordSuccess,RecordFailure,State}` unreachable [T]. | §1.3's "5 consecutive 403s on one entity → entity breaker opens; acting character re-elected" has no implementation on the live path. `internal/sync/worker` records a 403 against the acting character in the store, which re-elects, but no breaker opens. |

### Blocking Gate 2 (Revocation SLO)

| Defect | Evidence |
| :-- | :-- |
| **B27 — token lifecycle invalidation never runs.** `sso.NewLifecycle`, `Lifecycle.InvalidateForOwnerHashChange`, `Lifecycle.InvalidateForInvalidGrant` unreachable [T]; `scopes.NeedsReauthorization`, `MergeScopes`, `NewSet`, `Set.{Has,Missing}` unreachable [T]. | Gate 2.3's trigger matrix requires *every* reducing event to enqueue an urgent job. Rows 1–3 — token invalidated (`invalid_grant`), `owner` hash change, scope set reduced — have no producer at all. |
| **B26 — the RBAC mutation surface has no caller.** `rbac.{CreateRole,AssignUserRole,RevokeUserRole,AddRoleGrant,AddSquadRole,AddSquadMember}` and `rbac.Resolve` unreachable [T]; no role-management route is registered in `internal/api/v1/router.go`. | Trigger-matrix rows "RBAC role revoked" and "Squad membership removed" have no producer. It also contradicts `cmd/hangar/serve.go`'s own comment that "§4.9's outbox is written by internal/rbac's mutations" — those mutations never execute, so on a running installation the outbox has no writer. |

### Blocking Gate 3 (Alert Delivery Integrity)

| Defect | Evidence |
| :-- | :-- |
| **B25 — the alert pipeline delivers but never generates.** `alerting.Emitter.{IngestNotification,Emit}` unreachable [T]; `alerting/render.{Render,HasTemplate}` unreachable [T]; the dedupe surface (`Fingerprint.Hash`, `NotificationFingerprint`, `ThresholdFingerprint`, `SemanticFields`) unreachable [T]; coalescing (`NewCoalesceKey`, `CoalesceKey.{String,Due}`) unreachable [T]; `DeadLetterCount` unreachable [T]; `alerting/catalogue.{Thresholds,ThresholdSourceRoutes,ValidateThresholds,Names,CountByDomain,SeededTotal,SkyhookNames}` unreachable [T]. | `alerting.Dispatcher` (the outbox pump) **is** wired, by `cmd/hangar/alerting.go`. So delivery runs and nothing produces anything to deliver. Gate 3.1's accounting identity `generated == delivered + coalesced_into + dead_lettered + suppressed_by_dedupe` has no left-hand side; a 4-hour run drops zero alerts because zero alerts exist. Gate 3.2 (generic fallback renderer), 3.3, 3.4 (40 coalesced events render as one) and 3.6 (dedupe hashes stable across restart) each test a component with no caller. |

### Blocking Gate 4 (Feature Parity)

| Defect | Evidence |
| :-- | :-- |
| **B30 — ESI route handlers that are never dispatched.** `handlers.{SyncCalendarEventDetail,SyncCalendarAttendees,SyncMarketHistory,SyncMarketPrices,SyncPlanetColonyDetail,SyncCorporationProjectContributors}` and their `Parse*` counterparts — 13 functions — unreachable. | Gate 4.2 requires every one of the 106 measured distinct ESI routes to map to a live `app.sync_subscription` route or be recorded as deliberately unmapped. These are neither: they are implemented and never scheduled. |
| **B23** (recorded) — `internal/i18n` absent from the binary. | Gate 4.5 requires all 9 UI locales "each resolving to a valid ESI `Accept-Language`". The resolution runs only in tests. |
| **B22** (recorded) — `internal/sde` absent from the binary. | The `sde.*` tables can only ever be empty. |
| **B32 — entitlement-rule write endpoints are not registered.** `v1.CreateEntitlementRule`, `v1.DeleteEntitlementRule` unreachable [T], with tests in `admin_rules_test.go`. `RegisterAll` never mounts them. | Gate 2's trigger row "Entitlement rule deleted or narrowed" and Gate 4.3's controller mapping both depend on a surface that returns 404. |
| **B33 — `$filter` validation never runs.** `filters.New`, `filters.Validate` unreachable [T]. | The filter grammar is validated only in its own tests. |

### Blocking Gate 5 (Deployment Usability)

| Defect | Evidence |
| :-- | :-- |
| **B24 — §4.9 has no configuration surface.** `crypto.SealWebhookSecret`, `crypto.NewWebhookSecret` unreachable [T]. | Already known and in scope for this phase. `app.webhook_endpoint` rows can be created only by direct SQL, and the HMAC secret is envelope-encrypted with AAD bound to the endpoint's own uuid. |
| **B34 — the Redis L2 tier is never constructed.** `cache.NewRedisL2`, `redisL2.{Get,Set}` unreachable [T]. | `docker-compose.yml` ships a `cache` profile whose service, when enabled, is not used by anything. Principle 7 says Redis must be optional; it does not say the option should be inert. |
| **B35 — the TeamSpeak challenge flow has no caller.** `teamspeak.{IssueChallenge,RedeemChallenge}` unreachable [T]. | TeamSpeak identity linking cannot be completed on a running installation. |

### Not gate-blocking, but the same pattern

| Defect | Evidence |
| :-- | :-- |
| **B31 — `internal/esi/pagination` is a dead duplicate.** The whole package is absent from the binary. | `internal/sync/worker` carries its own complete page-walker with torn-set detection, and it is the one that runs. The two disagree: the dead one fans out at `MaxPageConcurrency = 4` (which is what 01_ARCHITECTURE.md §5.9 specifies), the live one is **serial**; the dead one's `detectTornSet` ignores a page with no validator, the live one treats a missing validator as torn, which is **stricter**. The live behaviour is the safer of the two but is not the specified one, and the cursor mechanism (`CursorQuery`, `FetchAllCursor` — §5.9's other pagination mode) has no live implementation anywhere. Resolve by making one of them the single implementation, not by deleting the evidence. |
| **B36 — the metric surface does not exist.** `telemetry.NewRegistry` has no caller and no `/metrics` endpoint is served. Every metric named in 04_RELEASE_GATES.md's instrumentation table — `esi_ledger_divergence`, `esi_ledger_mode`, `esi_replica_live_count`, `esi_429_total`, `esi_420_total`, `esi_error_limit_remaining`, `provisioning_revocation_latency_seconds`, `alert_delivery_total`, `alert_dead_letter_depth`, `esi_429_headerless_total` — appears **only in comments**. | SRS §0 already assigns the metric surface to Phase 20, so building it now is in scope. But 04_RELEASE_GATES.md's preamble says the opposite — "each gate's *instrumentation* lands in the phase that builds the subsystem. A gate whose metrics are added in Phase 20 cannot be measured retroactively" — and names phases 4, 11 and 14 as the owners. **That is a specification contradiction between the SRS and the gate document**, and it has to be resolved explicitly rather than by picking the convenient reading. Recorded here per Principle 15. |

---

## What this means for Gate 4

Gate 4.1 requires 58 capability rows at `status = verified`. The brief for this phase anticipated
two capabilities being un-delivered. The audit finds that the shortfall is broader: alert
generation, RBAC mutation, token-lifecycle invalidation, six ESI route handlers, SDE import,
locale resolution, entitlement-rule writes and TeamSpeak linking are each implemented, tested,
and unreachable from `main`.

**Gate 4 cannot be signed off in this state**, and per §4.2 a capability with no delivering phase
is a specification defect that blocks the gate rather than something to be marked verified with a
note. The traceability CSV is still worth producing — it is the artefact that makes the shortfall
countable — but it will carry `status = unreachable` rows, and that is a gate failure, correctly
recorded, not a gate pass.

Per rule §0.4, wiring these up during Phase 20 and then running the gates does not convert them
into passes either: "a gate that requires a code change to pass is a failed gate, not a fixed
one." The wiring is still worth doing — an inert subsystem should not stay inert — but the gate
result that follows it is evidence about the *next* release candidate, not this one.

---

## Closure register — Phase 20.8

The registers below this one are older; this is the newest.

Phase 20.8 closed the last two unreachable capability rows and the defect in the artefact that
was supposed to be counting them. Reading every row of that artefact carefully — which is what
writing forty-two verification tests forces — then turned up two more defects of the same class,
one column over and one layer down.

| Defect | Closed by | Verified by |
| :-- | :-- | :-- |
| **B51** — the traceability matrix cited 42 tests that did not exist | `test/capability`, one test per Appendix A row, named exactly as the row names it. Each asserts the FOUR links of the capability's delivery chain, and each link is one that has actually broken here: the route is DISPATCHED (B30/B47/B48), the DTO covers what the live spec marks required (B50), the RECORDED response parses to concrete values, and the `/api/v1` endpoint is REGISTERED (B52). The half that needs Postgres — user, scope, audit, squad, webhook, intel and note administration, all HANGAR-native database behaviour — is a build-tagged companion sharing one container. `tools/gate4-traceability/verification_tests.go` additionally now requires a cited test to make **at least three assertions**: 20.7's checker asked only whether the name existed, and once it does, a function named `TestSyncAssets` that asserts nothing satisfies that check and is worse evidence than the missing test, because it looks checked | `make gate4-evidence` exits **0**: 58 rows verified, 0 unreachable, **0 false citations**, every cited test above the assertion floor |
| **B52** — the matrix's OTHER hand-written column had the same defect | `hangar_endpoints` names the `/api/v1` endpoints delivering each capability, every value hand-written, nothing ever confirming the endpoint existed. Measured against `docs/openapi.json`: **7 of 22 matched no registered path, not even as a prefix** — `/api/v1/admin/audit` (the log is `/admin/security-log`), `/admin/pin` (`/admin/esi/catalogue/pin`), `/admin/routes` (`/admin/sync/routes`), `/admin/tokens` (API tokens are user-scoped: `/api-tokens`), `/admin/webhooks` (`/me/webhooks`), `/market/prices` (singular — the B38 shape again), and `/api/v1/meta/locales`, **which has never been registered in any spelling**. Capability #58 is Localisation and its row claimed delivery by an endpoint that does not exist; HANGAR's locale is a boot setting (`HANGAR_LOCALE`), not a served resource, so the row's honest answer is "no endpoint" and it now says so. `capability_endpoints.go` measures it and fails the gate on the count | `make gate4-evidence`; `requireEndpoints` in `test/capability` asserts the same thing per capability, from the other side |
| **B53** — `is_corporation` was the caller's to say, and it was not | `handlers.MarketOrderDTO`'s comment asserted "`is_corporation` does not appear on either owner's response at all; the caller supplies it". Measured against the live spec: the CORPORATION order routes do not declare it (correct — every order there is a corporation order), and **both CHARACTER order routes mark it REQUIRED**. `CharacterWorker` passed a hard-coded `false`, so a director's corp-placed order — which appears in their own order list with the flag true — has been recorded as personal on every installation. The DTO now carries it as a POINTER, so "ESI said false" and "ESI said nothing" stay distinguishable, and `orderIsCorporation` prefers the body over the caller's endpoint flag | Found by `requireDTOCoversSpec`, which compares the DTO against the SPEC rather than against its own field names — B50's lesson made mechanical. Pinned by `TestSyncMarketOrders` |
| **#41** — structure/station resolution, unreachable since Phase 15 | `app.location` had a table, an endpoint and no writer, and the missing piece was never the handler — it is thirty lines. It was the ENUMERATION: neither route can be listed, so the id set has to come from HANGAR's own rows and no query produced one. `ListUnresolvedStationIDs` and `ListCharacterStructureIDs` read the three tables where **ESI itself states the kind** (`app.asset` and `app.character_clone` carry CCP's `location_type`; `app.character_location` splits `station_id` and `structure_id` into separate columns), plus `app.corporation_structure`, whose whole contents are structures by construction. Contract and market-order location ids are deliberately NOT read: they carry no discriminator, and guessing station-vs-structure from the numeric range is the coercion Principle 13 forbids. The two halves are different entity kinds for a measured reason — a station is unauthenticated (one GLOBAL pass), a structure needs `esi-universe.read_structures.v1` and per-character docking access (CHARACTER-scoped, over that character's own rows) | `TestSyncLocationResolution`; `TestStationRouteIsUnauthenticatedAndStructureRouteIsNot`; `TestGlobalRoutesRequireNoScope` |
| **#37** — the alliance table where every name was the empty string | `app.alliance`'s only writer was `UpsertAllianceStub`, which inserts an id and an empty name, so `GET /api/v1/alliances` served a list of blanks on every installation ever deployed. 20.8 built the FOURTH worker: `worker/alliance.go`, the `EntityAlliance` branch of `internal/sync/election.go` (an alliance has no token, so §6.3's election applies with a different candidate pool — every tracked character whose CORPORATION is in the alliance), and `ReconcileAllianceSubscriptions`. All four routes are plain dispatch entries, not fan-outs: the alliance id is their only path parameter and that is what `entity_id` holds. The member-corporation route reconciles `app.corporation.alliance_id` for corporations HANGAR ALREADY HAS, in both directions, and **inserts nothing** | `TestSyncAlliance`; `TestAllianceRoutesAreDispatchedNotFannedOut`; `TestSyncAllianceSubscriptionsAreOrdered` (integration: the elector's pool, and that a member id HANGAR has no row for is not stubbed). **NOT live-verified, and cannot be here**: `app.alliance` holds 0 rows because HANGAR Corp is in no alliance, so the reconciler creates no subscription and the worker is never dispatched. Putting a tracked character into an alliance is an operator action |

**A latent hazard was found while placing #37 and is now machine-checked.** The three subscription-reconcile statements are not symmetric: the character and corporation ones carry a `NOT EXISTS` scope gate, and `ReconcileGlobalSubscriptions` **cannot** — a global row has `acting_character_id = NULL`, so there is no token to gate on, and `DisableUnscopedSubscriptions` reads a NULL acting character as fully covered. A SCOPED route placed in the global set would therefore be created *enabled* and 403 on every attempt forever, against Governor 2's installation-wide budget of 100 errors a minute. The invariant held by accident until this phase and was asserted only in a comment. It is the reason capability #37's contacts routes went to a new worker instead of a cheaper global fan-out, and the reason #41's two halves are split. `TestGlobalRoutesRequireNoScope` and `TestScopedSubscribableRoutesAreOwnerScoped` hold it now.

---

## Closure register — Phase 20.2

Recorded here rather than by editing the findings above, so this document keeps meaning "what
the audit found" and not "what is currently broken". Each row names the wiring, not the
intention.

| Defect | Closed by | Verified by |
| :-- | :-- | :-- |
| **B23** — `internal/i18n` absent from the binary | `HANGAR_LOCALE` (installation-wide, default `en`), validated at boot by `internal/config.Validate` against `i18n.UILocales()`, resolved by `cmd/hangar`'s `buildGateway` into `esi.Client.Language` — which now also SENDS `Accept-Language`, which it never had | `TestNoPackageIsAbsentFromTheBinary`; `TestLocaleIsValidatedAtBoot`; `TestEveryUILocaleIsAcceptedByValidate`; `TestResolvedLanguageIsSentAndKeyed` |
| **B28** — the entity circuit breaker never runs | `esi.Client.EntityBreaker`, consulted per `(route, EntityID)` in `Do`, failing only on 403 and resetting only on 2XX/3XX; workers pass the character or corporation id | `TestEntityBreakerIsScopedToTheEntity`; `TestEntityBreakerIgnoresStatusesThatSayNothingAboutAuthorisation`; `TestEntityBreakerOpensAfterFiveConsecutive403s` (integration, against the Gate 1 proxy) |
| **B29** — rate-limit header reconciliation never runs | `Client.Do` calls `ratelimit.ClassifyResponse` once; reconciliation, the 429 snooze and the headerless signal all read its `Outcome`. `IncrementErrorBudget`/`ResetErrorBudgetWindow` DELETED as superseded races, not wired | `TestReconcileUsesTheServersOwnCeiling`; `TestReconcileFallsBackToTheUNREDUCEDCeiling`; `TestAbsentRemainingHeaderIsNotAReadingOfZero`; `TestClientReconcilesAgainstServerHeaders`; `TestHeaderless429SnoozesAndCounts`; `TestRetryAfter429SnoozesForExactlyThatDuration` |
| **B31** — `internal/esi/pagination` is a dead duplicate | It is now the single implementation. Concurrency 4 adopted (the spec's value); the torn-set check tightened to the live, stricter reading; the cursor walker implemented and wired to `/corporations/{id}/projects` | `TestNoPackageIsAbsentFromTheBinary`; `TestMissingValidatorMidSetIsTorn`; `TestNoValidatorAnywhereIsNotTorn`; `TestFetchAllPagesFansOutAtFour`; `TestFetchAllCursorPagesFollowsTheCursor` |
| **B39** — blank unauthenticated page | **Not reproducible.** A root `errorComponent`/`notFoundComponent` added regardless — its absence is what made any throw on the unauthenticated path render nothing at all | manual, cold browser context; see the phase report |
| **B40** — a fresh SSO user holds zero permissions | `rbac.BootstrapFirstAdmin`, called from the SSO callback's `OnUserAuthenticated` hook, gated on `CountSuperuserHolders() == 0` inside one transaction | `TestFirstLoginPromotesExactlyOneAdministrator`; `TestBootstrapRespectsACuratedAdminRole`; `TestBootstrapIsSuppressedByADeniedSuperuser` |
| **B26** — the RBAC mutation surface has no caller | **Surface half closed early** (B40 could not be fixed without it): `internal/api/v1/admin_roles.go` registers role create/delete/get, per-grant add/remove, user-role assign/revoke, the permission vocabulary and `GET /api/v1/me/permissions`; the squad endpoints now go through `internal/rbac`'s wrappers instead of the raw queries | both reachability guards; `admin_roles.go`'s routes appear in `docs/openapi.json` |

**Two symbols from B26 are RECLASSIFIED rather than closed.** `rbac.Resolve` and
`rbac.ResolveLive` remain unreachable and have moved to the allowlist's "not defects" section.
Production resolves in BULK — `RefreshUser` → `ResolveAllLive` → `ResolveAll` — because
materialising one user writes the whole closed set in one pass. The single-permission forms are
the truth-table test surface and the documented materialisation cross-check; the middleware
deliberately never calls them, because it reads the materialised row and a second live path could
disagree with what is about to be enforced. `GetUserByMainCharacterID` likewise stays: nothing in
HANGAR looks a user up by their main character, and inventing an endpoint to give a query a caller
would be the allowlist wagging the codebase.

## Closure register — Phase 20.3

| Defect | Closed by | Verified by |
| :-- | :-- | :-- |
| **B27** — token lifecycle invalidation never runs | `internal/sso.Lifecycle` wired to `sso.Flow.OnOwnerHashChanged` (`serve`) and to `Refresher.OnInvalidGrant`/`OnOwnerHashChanged` (`work`), with `cmd/hangar/revocation.go`'s `revocationNotifier` bridging to `provisioning.Urgent`. The `invalid_reason` vocabulary moved to one place (`sso.ReasonInvalidGrant`/`ReasonOwnerHashChanged`) so refresh.go's in-transaction invalidation and Lifecycle cannot drift. New `Flow.OnScopesReduced` is Gate 2 trigger row 3's first producer of any kind. `internal/scopes` reached through the reauthorize scope MERGE and `GET /api/v1/me/characters`'s `needs_reauthorization`/`missing_scopes` | `TestLifecycleNotifiesEveryReducingEvent`; `TestScopeReductionFiresTheHook`; `TestUnchangedOrWidenedScopesDoNotFireTheHook`; `TestOwnerHashChangeInvalidatesTokens`; both reachability guards |
| **B32** — entitlement-rule write endpoints not registered | `internal/api/v1/admin_rules.go`: `POST /api/v1/admin/platforms/{id}/rules` and `DELETE .../rules/{rule_id}`, the latter driving the bulk urgent revocation with a single `eventAt` for the whole fan-out | `docs/openapi.json`; both reachability guards |
| **B35** — the TeamSpeak challenge flow has no caller | `internal/api/v1/teamspeak_link.go`: issue (user), status (user), redeem (privileged observer). Automatic `clientlist` observation is explicitly NOT implemented and is recorded in that file's header | `docs/openapi.json`; both reachability guards |
| **Gate 2's metric** | `telemetry.RevocationLatency`, a histogram observed in `provisioning.UrgentWorker` from `CompleteProvisioningAudit`'s own `RETURNING extract(epoch FROM (platform_call_completed_at - event_at))` | `TestGate2HarnessMeasuresWhatTheGateDefines` (the metric and the table must agree) |
| **Gate 2's §2.1 harness** | `test/load/gate2_revocation.go` + `gate2_integration_test.go` | `TestGate2HarnessMeasuresWhatTheGateDefines`; `TestGate2EmptyRunDoesNotPass`; `TestGate2DownPlatformKeepsItsExposure`; `TestGate2UrgentEnqueuesInTheMutatingTransaction` |

**One symbol is RECLASSIFIED rather than closed.** `internal/scopes.Set.HasAll` moves to the allowlist's
"not defects" section. Production wants the LIST every time — a user is told *which* scopes are missing,
never merely that some are — so `Missing` is the answer and `HasAll` is the predicate the truth-table
tests assert against. That is the same relationship `rbac.Resolve` has with `ResolveAllLive`, reclassified
in 20.2 for the same reason. 20.2's two reclassifications (`rbac.Resolve`, `rbac.ResolveLive`) and
`GetUserByMainCharacterID` are **not** disputed and stay listed.

### Findings made DURING 20.4, not by the original audit

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| **`app.platform.locked_down` was never read by anything** | Phase 15.1 added the column, `POST /api/v1/admin/platforms/{id}/lockdown` that writes it, the security-log entry, and the admin UI badge. Migration 00040 describes it as "the incident switch: it stops all outbound provisioning for the platform". `grep -rn LockedDown internal/` outside the generated code and the handler returns nothing: `ListEnabledPlatforms` filters on `enabled` alone, `UrgentWorker.Work` looked the driver up and called it, and `reconcileOneUser` did the same. An administrator freezing a compromised Discord integration during an incident received an audit trail saying they had frozen it and a platform that carried on being written to | **Fixed in 20.4.** `provisioning.PlatformIsLockedDown`, read at the moment of the outbound call (not at enqueue time — a revocation queued a second before the switch must not go out, and one queued during the freeze must go out the moment it lifts), with a new `skipped_locked_down` audit outcome. `actual_groups` is left untouched, so the exposure stays on the exposure board with its true age. A security control that records its own operation and does not perform it is worse than one that is absent |
| **Every single-event alert was delivered as a folded one-liner** | `render.Render` — "the full body for a message carrying a single event", per its own Phase 14 doc comment — had no caller. `Dispatcher.message` always used `render.Line`, which folds the generic key/value listing onto one line so a roll-up can list one line per event. For a group of ONE that squashes a readable multi-line listing into `key value; key value; key value`. That is most alerts on a normal installation and every alert that matters urgently: a structure comes under attack once, not forty times in five minutes | **Fixed in 20.4**, and it is why `render.Render` came off the allowlist rather than being reclassified — the symbol was unreachable because the output was wrong, not because the function was surplus |
| **`internal/events.PendingCount`'s allowlist entry named the wrong metric** | It was listed as "outbox depth query awaiting the alert_dead_letter_depth metric (20.4)". It counts UNDISPATCHED §4.9 WEBHOOK OUTBOX events (`app.outbox_event`); `alert_dead_letter_depth` is a gauge over `app.alert_delivery` and its only correct source is `alerting.DeadLetterCount`, which is what 20.4 wired | **Corrected in the allowlist, not closed.** A webhook-outbox backlog gauge would be a real and useful metric, but no gate names one, and release-gate rule 7 is about not declaring metrics ahead of their wiring — not a licence to invent metrics nobody asked for. Recorded here so the next phase does not "fix" it by wiring the wrong number to the right name |
| **Threshold alerts can be silently unfirable on a specific installation** | §4.4 makes "a threshold alert whose source route is not in the sync set" a BUILD-time error, and `catalogue.ValidateThresholds` enforces exactly that. It cannot see the runtime half: the route is in the sync set, in the catalogue, unblocked — and THIS installation has no enabled `app.sync_subscription` for it, because no character granted the scope. The alert then generates zero alerts on an installation where nothing is wrong with the alert | **Fixed in 20.4.** `alerting.Evaluator.reportUnpollableThresholds` says so once, at startup, naming the alert types the operator will not receive rather than only the route they have never heard of |
| **`EmitRequest` had no way to say who an occurrence was FOR** | Routing resolves by alert TYPE only (`ListAlertRoutingRulesForType`), which is right for a CCP notification — a routing rule is an audience. A THRESHOLD has a subject: `corporation.structure.fuel_low` is about one structure belonging to one corporation. On an installation serving two corporations, a rule targeting `corporation:98000001` would have received the other corporation's fuel alerts, with nothing in the payload to reveal it but a structure id | **Fixed in 20.4** by `EmitRequest.TargetFilter`, deliberately narrow: it can only drop a target whose kind carries an entity id that CONTRADICTS the subject's owner. `user`, `squad` and `installation` targets are audiences with no ownership claim and are never filtered |

| **`esi_ledger_divergence` now measures PREDICTION ERROR, and §1.3's words describe the POST-CORRECTION residual — which is 0** | Found by watching the live installation with the 20.4 fix in place. `corp-contract` held a reading of **18** for minutes; `corp-member` touched **−24**. Reading the bucket out directly explains both: 8 settled entries of cost 1 (all 304s) totalling 8 tokens, against a server reporting 26 consumed, and one SYNTHETIC entry of cost 18 injected by the reconcile that observed the gap. Measured across all 18 buckets in one statement: `max(abs(local_at_reading − server_remaining))` = **18**, and `max(abs(live_local − server_remaining))` over buckets reconciled in the last 5 s = **0** | **RESOLVED IN 20.4.1, by splitting the metric rather than by choosing between the two readings.** `esi_ledger_divergence` is now the POST-correction residual — Gate 1.3's quantity, bound **0 tokens**, non-zero only when the reconciler could not converge; `esi_ledger_prediction_error` is 20.4's pre-correction gap, RECORDED per group in `divergence.csv` and deliberately not bounded, because reconciliation excludes in-flight reservations by design and `k` unsettled siblings put up to `5k` tokens between the operands. `04_RELEASE_GATES.md` §1.2 carries the amendment, the measurement, and the units answer: the condition's bound was stated in REQUESTS and its measurement in TOKENS, and 0 tokens is strictly TIGHTER than the one request the sentence allowed, so the gate narrows rather than relaxes. Migration 00043 adds `local_remaining_after_reading`; eviction was made exact so the residual's bound really is 0 (see the 20.4.1 table below). **The mechanism stated in this row's own evidence was wrong on one point and it is corrected below**: the 18 tokens are NOT consumption HANGAR evicted early — the window was measured in 20.4.1 and HANGAR's is not shorter than ESI's |

| **A FRESH installation seeds 50 of its 54 alert types, and the four missing ones cannot be routed** | The four THRESHOLD rows carry a NOT NULL `source_route_id` enforced by the `threshold_declares_source` CHECK, and `db/seed/alert_types.sql` inserts them through a JOIN against `app.esi_route` — which is empty until the catalogue is ingested. `migrate up` applies seeds BEFORE any ingest. Verified on the 20.4 release image against a throwaway Postgres: the first `migrate up` gave 50 types and 0 thresholds; after the ingest landed 225 routes, a second `migrate up` gave 54 and 4 | **FIXED IN 20.4.1.** `cmd/hangar`'s `ingestCatalogue` re-applies the seed set after a successful ingest, so the dependency completes itself on the first boot rather than on whichever later `migrate up` happens to run. Both `migrate up` and the ingest now REPORT the threshold count, naming the four types when there are none. Not a crash, which is what let it survive four phases — `app.alert_routing_rule` has a foreign key to `app.alert_type`, so the operator simply could not create a routing rule for them, and the evaluator then reported them unrouted, skipped them, and looked healthy while being structurally incapable of firing: §4.4's own "silently generates zero alerts", exactly |

### Findings made DURING 20.4.1, not by the original audit

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| **`esi_ledger_divergence` had NO SOURCE AT ALL in solo mode, and the Gate 1 harness read that as a pass** | Both readers of the gauge read `app.esi_ledger_bucket`. Only the CLUSTERED ledger writes it: `UpsertLedgerBucket` from the clustered acquire and from a mode-transition flush, `RecordServerLedgerReading` from the clustered reconcile. `LedgerSolo.Reconcile` touches memory and nothing else. Reproduced on the live installation by running `work` alone (one live replica, so `esi_ledger_mode{mode="solo"} 1`): after two minutes with 15 jobs running, every `server_observed_at` in `app.esi_ledger_bucket` was still an hour old, from the previous clustered run — so the freshness rule dropped every row and the gauge emitted zero samples. `test/load/gate1_esi.go`'s `maxDivergence` over zero samples is 0, and 0 is condition 1.3's passing value. §1.4 requires the gate to run at N=1 **and** N=3, so half of Gate 1 was passing on an empty gauge | **FIXED IN 20.4.1**, at both ends. The solo ledger now records each reconcile's before/after pair in the bucket it already holds, and `Governor1.SoloReadings` offers it to the collector at SCRAPE time — no database round-trip is added to the path whose whole purpose is not having one, and there is deliberately no merging of the two sources, because exactly one ledger is authoritative at any instant. The harness now fails condition 1.3 when it never observed the gauge, and says so in the measurement instead of printing a maximum. Verified live: 12 groups reporting in solo mode where the previous build reported none |
| **Eviction over-forgave up to 4 tokens per correction, in the direction that causes a breach** | `evictUntil` (clustered) and `evictOldestUntil` (solo) deleted whole entries until availability REACHED the server's figure. Costs are 1, 2 and 5, so a cost-5 entry at the head of the queue closing a 1-token gap forgave 4 tokens ESI had not forgiven | **FIXED IN 20.4.1.** Both ledgers reduce the boundary entry's cost by exactly the remainder (`ReduceLedgerEntryCost`; the entry keeps its `consumed_at` and still ages out on its own schedule). Found while establishing whether the post-correction residual's bound is 0 or 4 — so it surfaced as a metric question and is a CORRECTNESS fix: over-forgiveness leaves HANGAR believing it holds headroom ESI has not granted, which is Gate condition **1.1**, not 1.3 |
| **900 s is the SPEC's window and not a HANGAR default; ESI's window is the same LENGTH but a different SHAPE, with a longer tail** | Traced first: `internal/esi/catalogue`'s ingest reads `x-rate-limit.window-size` and `time.ParseDuration`s it; every one of the 225 ingested routes carries `"15m"` (`SELECT DISTINCT spec_fragment->'x-rate-limit'->>'window-size'` returns exactly one row), and live ESI's own `X-Ratelimit-Limit` says `3600/15m`. Then measured against real ESI on two public groups no HANGAR subscription polls. **Cost model:** `X-Ratelimit-Used` reports 2 for a 200 and 1 for a 304 — §5.5's table exactly, so the 20.4 gap was never a pricing mismatch. **Release schedule (`insurance`, 150/15m):** a 40-token burst at 01:31:11 returned NOTHING at t=120 s, 300 s, 600 s or 780 s — remaining tracked the probes' own cost to the token — and there was no step at t=900 s either. Recovery began between 01:44:12 and 01:45:44, i.e. at the **01:45:00 wall-clock boundary**, and then proceeded linearly: 100 → 103 → 106 → 112 across t=1005…1353, about 48 tokens per 900 s, which is the previous window's entire count decaying away. The fit was then confirmed across a SECOND roll: at 02:00:00 the 01:30-window's 48 had fully decayed and remaining read **128 = 150 - 22**, 22 being the 01:45-window's own probe consumption, exactly as a sliding-window counter predicts. **Independently on `killmail` (3600/15m):** consumption tracked 1:1 with zero release for 285 s and then began recovering at **01:15:00**, also a boundary. Two bursts on two groups, each beginning to recover at a `:00/:15/:30/:45` boundary rather than at its own 900 s anniversary | **NO CODE CHANGE, and that is the finding.** The constant is right — it is CCP's own, from two independent sources, and it had never been checked. The shapes differ: HANGAR releases each request's cost one window after THAT request (§5.5's model, with continuous refill explicitly prohibited); ESI behaves as a **sliding-window counter over fixed 15-minute wall-clock windows**, charging a request in full until the boundary and then decaying it over the following 900 s. Mean retention is the same ~900 s, but ESI's **tail is longer** — between 900 s and 1800 s after a request it is still charging a fraction HANGAR has forgotten, which is the drift the reconciler answers with a synthetic entry. Matching ESI's shape was considered and REJECTED: a windowed counter discards a whole window's consumption at one instant, which is more optimistic than the floating window at exactly the moment that matters, and is what §5.5's prohibition exists to refuse. Recorded in `01_ARCHITECTURE.md` §5.5 and `02_DATABASE_SCHEMA.md` §4.3, because "we checked, and 900 s is correct" was worth having written down and was written down nowhere |
| **The 20.4 finding's stated mechanism for the 18-token gap was RIGHT in direction and wrong in cause** | That row said the 18 tokens were "consumption HANGAR had already EVICTED from its 900 s floating window and ESI still counted — the two windows are not the same window", and treated in-flight fan-out siblings as a secondary aside. The measurement above says the windows are the same LENGTH, so "HANGAR's window is too short" — the reading that would have made this a constant to repair — is ruled out. Both named effects are real and they add: ESI's longer tail keeps charging what HANGAR has released, and reconciliation deliberately excludes in-flight reservations, so every sibling ESI has processed and HANGAR has not yet settled sits between the operands at up to 5 tokens each. `corp-contract` is a fan-out group (`/corporations/{id}/contracts/{contract_id}/items`), which is why it, and not a single-request group, held 18 | **CORRECTED, not fixed** — there was nothing to repair, and establishing that is the point: the two readings imply opposite actions, and 20.4.1 would otherwise have spent itself changing a constant that is CCP's own. `esi_ledger_prediction_error` is now the metric that carries this quantity. Its magnitude is therefore NOT purely a function of in-flight concurrency against real ESI, which is a second reason `04_RELEASE_GATES.md` §1.2 records it rather than bounding it |
| **`HANGAR_METRICS_ADDR` defaults to one port for every role, and the role whose metrics matter most loses the race** | `serve` and `work` both default to `0.0.0.0:9090`. On a single box the second to start cannot bind, logs it, and carries on — deliberately non-fatal. But `work` is the only role that builds an ESI gateway or runs the alert dispatcher, so `esi_ledger_mode`, both ledger gauges' solo source and the whole of Gate 3's metric set exist only there, and which process loses is a startup race. Phase 20.4 hit this and worked around it by hand | **DOCUMENTED, not defaulted differently, in 20.4.1.** A per-role default would fix the development topology by breaking the shipped one: in compose each role is its own container with its own network namespace, so one port number is correct and three would be a regression for every operator. Instead the bind-failure log now names the other roles and says which metrics this process was the only source of, and `.env.example` carries the per-role override where somebody configuring a single box will read it |
| **`make bench-ledger-clustered` still fails through Windows, still for the reason 20.4 measured** | 10.099 ms p99 against the 10 ms budget, on the tree with 20.4.1's changes in it. Re-measured the way 20.4 established as authoritative — inside a `golang:1.26-alpine` container on the WSL2 Docker host, Postgres reached over the Linux bridge instead of Windows port forwarding — four consecutive runs gave p99 **7.255, 7.052, 5.848, 6.329 ms** at **11,220–11,616 ops/s**, against a 10 ms budget and a 2,000 ops/s target. That is inside 20.4's own band (6.863–7.763 ms) with slightly better throughput | **No action, and no regression.** Recorded because the benchmark was re-run for this phase and a bare "it failed" would read as one. `BenchmarkLedgerClusteredThroughput` exercises Acquire and Settle only; every 20.4.1 change is in Reconcile and eviction, which it never calls. The Windows number is also *better* than the 10.13–12.50 ms baseline the same machine produced at commit `88381d6` before any of this work |

| **A live database carried a verification fixture that fails by design** | 20.4 left an alert channel `phase20-4-live-sink` pointing at `http://127.0.0.1:8123/hook` — a local Python sink that is no longer running — and one ENABLED routing rule sending `CorpAppNewMsg` to it from the `installation` target | **DELETED in 20.4.1**, both rows. Kept, every future `CorpAppNewMsg` would retry, exhaust its attempts, dead-letter, and drive `alert_dead_letter_depth` up for a reason nobody would remember. A fixture that proves a delivery path once does not belong in the installation afterwards; 20.4 already recorded the end-to-end delivery it was built for |


## Closure register — Phase 20.5

| Defect | Closed by | Verified by |
| :-- | :-- | :-- |
| **B22** — `internal/sde` absent from the binary | `hangar admin import-sde` (with `--from-dir`, `--from-zip` and `--if-changed`) and `hangar admin sde-status`, plus a boot-time report from `serve`. The trigger question is answered in `cmd/hangar/admin_import_sde.go`'s header: an OPERATOR COMMAND, not a startup step (a multi-gigabyte download from CCP's CDN must not gate boot, and the swap takes an ACCESS EXCLUSIVE lock) and not a scheduled job (the export changes per patch; `--if-changed` compares CCP's manifest build against the last verified import and exits 0 having done nothing, so an operator who wants a cadence has a one-line cron and HANGAR has not invented a policy about when to lock their database). **A never-imported installation renders THE ID**: `ListSdeTypeNames` returns no row for an unknown type and `renderEFT` keeps its `[<type_id>]` placeholder; the three skyhook/sovereignty-hub backfills are `EXISTS`-guarded and leave their columns NULL. Never a blank, never a guessed constant | `TestAnInstallationWithNoSDEDegradesToIdsAndNeverToBlanks` (integration: asserts the degradation AND the recovery); `TestNoPackageIsAbsentFromTheBinary`, whose `known` map is now EMPTY; live: `admin sde-status` on the installation, and the character asset tree rendering `Type 3638` beside the named `Ibis - CEODude` |
| **B24** — §4.9 has no configuration surface | `internal/api/v1/webhooks.go`: create / list / rotate / revoke plus the event-type vocabulary, all behind the existing `webhooks.manage` permission. The secret is envelope-encrypted with AAD bound to the endpoint's uuid and **returned exactly once**, by the call that mints it; no read endpoint returns it and there is deliberately no "show me the secret" endpoint to add later. **A rotation is an OVERLAP, not a swap** — migration 00044 keeps the superseded secret for 24h and every delivery in that window carries both signatures (`t=...,v1=<new>,v1=<previous>`), so **an event already in the outbox when the rotation happens is neither dropped, delayed, nor re-signed**: it goes out dual-signed and verifies against either secret | `TestADualSignedDeliveryVerifiesAgainstEitherSecret`; `TestANilOrEmptyExtraSecretIsOmitted` (steady state is byte-identical to Phase 19's header); live: a real `rbac.user_role.revoked` delivery captured on the wire with **two `v1=` elements**, both accepted by `deploy/verify-webhook-signature.sh` — which read only the first and is fixed here |
| **B30** — thirteen ESI route handlers no worker dispatches | **The defect was not a missing handler.** Every fan-out route had a handler, a `case` in its worker, and passing tests; `worker/syncset.go` excluded all of them from `SubscribableRoutes`, so `internal/sync/subscribe` could never create a subscription naming one and the `case` was unreachable on every installation ever deployed. `fanoutRoutes()` makes them subscribable and `SyncSet()` is now derived from the one list instead of restating half of it. Measured live: **70 to 85 enabled subscriptions**, and thirteen fan-out routes executing against real ESI for the first time | `TestEveryFanoutRouteIsSubscribable`, `TestNoSubscribableRouteIsOrphaned`, `TestSyncSetIsExactlyTheSubscribableSet`, `TestB30RoutesAreAllReachable`; live sync_run rows for every fan-out path, and `/markets/prices` landing **15,820 rows** |
| **B33** — `$filter` validation never runs | `api.ValidateQueryFilters`, attached by `internal/api/v1`'s `get`/`mutate` helpers to EVERY operation, with the closed set derived by `filters.SpecFromQueryTags` from the operation's own `query:` struct tags — the same tags huma builds the OpenAPI parameter list from, so the enforced set and the documented set cannot drift | `internal/api/filter_whitelist_test.go`; live before/after against the same binary pair (see the measurement below) |
| **B34** — the Redis L2 tier is never constructed | `cmd/hangar.buildL2`: `HANGAR_REDIS_URL` REPLACES the Postgres L2 (01_ARCHITECTURE.md §5.4's DECISION verbatim), a malformed URL is fatal at boot, an unreachable one is not. **A Redis outage mid-request is a MISS** — not a failure, and not a fall-through to Postgres, because when Redis is the L2 nothing has been written to `app.esi_cache_entry` and falling through would serve a body the configured tier does not contain | `TestSameResultsWithTheTierAbsentColdWarmAndDead` (four configurations, one expected observable outcome, Redis terminated mid-suite); `TestTheNoStoreContractHoldsOnRedisToo` |
| **B38's leftover** — the contributors DTO mismatch | `handlers.ParseCorporationProjectContributors` now reads the live spec's shape. **`id` is the contributor's CHARACTER id** (`$ref: CharacterID`) and **`contributed` is int64 PROGRESS, not isk** (the project's isk field is `reward_isk`; its progress fields are `current_progress`/`target_progress`, which is the scale this is denominated in). One response feeds BOTH tables — the roster row and the amount row — because there is one route and 00011 built two tables as if there were two. `joined_at` stays NULL (ESI does not report it and `now()` would be a fabricated fact that then ages); `name` is not persisted anywhere, because inventing `app.character` rows from a display string would manufacture characters. The route is `x-pagination: cursor`, so it goes through `fetchAllCursorPages` | `internal/sync/handlers/project_contributors_test.go`, including that the OLD bare-array shape is now a parse error; the invented `project_contributions.json` golden fixture is DELETED and replaced with the real envelope |

**Two symbols are RECLASSIFIED rather than closed, and one query is DELETED.** `RevokeWebhookEndpoint` is gone rather than given a caller: `RevokeWebhookEndpointForOwner` supersedes it by putting ownership in the predicate instead of in a Go-side check somebody can omit, and closing a defect by wiring the weaker of two queries would be the allowlist wagging the codebase. The five B30 entries left in `generated_allowlist.txt` are READERS awaiting an endpoint, not writers awaiting a dispatch — and `ListCorporationProjectContributions` now has data to return for the first time.

### Findings made DURING 20.6, not by the original audit

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| **B46 was five endpoints, not two, and one of them returned 200** | Probed the running installation with CEODude's session rather than reading the report. `wallet/journal`, `wallet/transactions` (character and corporation) **and `GET /characters/{id}/mail`** all returned the identical `22P02`; `db/queries/mail.sql` had the same uncast row comparison and the same duplicated argument at the call site. `GET /characters/{id}/notifications` returned **200** and was broken differently: the handler encoded `api.Keyset{"before": <the whole row struct>}` and decoded the key `"sent_at"`, which is never present, so the old `cursorTime` helper returned the start-of-set sentinel and the "next page" re-served page one forever | **ALL FIVE FIXED IN 20.6.** The keyset is now `(time, id)` everywhere, the sentinel is a typed PAIR, and a cursor that is present but undecodable is a 400 rather than a silent restart — the behaviour that hid the notifications loop |
| **The live installation's own wallet data contains the tie the fix is about** | `app.wallet_journal` holds journal_id `25893163003` and `25893163002`, both stamped `2026-08-06T17:10:55` — a corporation registration fee and its matching credit. A date-only cursor landing between them skips the second permanently | **Used as the verification.** A hand-built cursor naming the first tied row returns the second, proving the tiebreak splits a real tie on real data rather than a synthetic one |
| **B47's dispatch gap had propagated into the SSO scope registration** | `tools/scopedump` derives the scope set from `worker.SyncSet()`. With corporation assets absent from every dispatch table, `SyncSet()` never named it, so `esi-assets.read_corporation_assets.v1` never reached `docs/SSO_APPLICATION.md`'s "46, and why each". Confirmed by two independent derivations agreeing at **47** after the fix (the live catalogue in `app.esi_route`, and the embedded spec snapshot via `scopedump`), against **46** granted on CEODude's token | **CLOSED IN 20.6.** The doc was corrected, the operator enabled the scope in the developer portal, and CEODude re-authorized on 2026-08-15. The grant went 46 -> 47, the running `serve` reconciled on its own periodic pass, and corporation assets became the **88th** subscription — polled `200`, ETag stored, next poll on the route's TTL. It landed **0 rows, correctly**: the cached body for that second is `[]` (2 bytes) because HANGAR Corp was founded nine days earlier and owns nothing. Confirmed by reading the response body, not the row count — 0 rows from an empty upstream and 0 rows from a broken sync are indistinguishable from the count alone |
| **Eighteen catalogued routes back an Appendix A capability that has a table, store queries and an `/api/v1` endpoint — and no writer** | Found by listing every `INSERT`/`UPDATE`/`DELETE` query and counting non-generated, non-test callers: **27 of 247 have none**. Intersected with the catalogue, that is killmails (`UpsertKillmail`, `…Attacker`, `…Item`), fittings (`UpsertCharacterFitting`, `…Item`, `DeleteCharacterFittingsNotIn`), contact notifications, insurance prices, `app.location` (structure/station resolution), corporation project detail and per-character contribution, regional market orders and types, the alliance sheet (`UpsertAlliance` — only `UpsertAllianceStub` is called, which writes an id with an empty name), and `/meta/status`. Verified live: `GET /api/v1/characters/{id}/fittings`, `/characters/{id}/killmails` and `/corporations/{id}/killmails` all return `"data":[]` against tables holding 0 rows | **RECORDED AS `ReasonNotBuilt`, NOT FIXED.** These are unbuilt capabilities, not missing map entries — B47's class is "handler exists, dispatch entry does not", and these have no handler. Per `04_RELEASE_GATES.md` §0.4 they are Gate 4 failures **correctly recorded**: `internal/sync/worker/unmapped.go` names each one with the capability that claims it, and the traceability CSV counts them |
| **`GET /api/v1/meta/esi-status` reports `"healthy": true` unconditionally and never asks ESI** | Capability #45 is "ESI Service Health (`/meta/status`)" and Gate 4.6 requires `/meta/status` and `/status` be present as two distinct capabilities. `/status` is in `globalDispatch` and lands in `app.setting`. `/meta/status` is in the catalogue and in no dispatch table; the endpoint answers from `ListBlockedEsiRoutes` with a hard-coded `healthy` field. Found because `TestEveryCatalogedGetRouteIsClassified` refused a comment claiming it was mapped | **RECORDED AS `ReasonNotBuilt`.** The field is asserted rather than measured and cannot ever report unhealthy |

### Findings made DURING 20.5, not by the original audit

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| **EVERY SUBSCRIPTION WAS SPINNING: `next_due_at` could be permanently in the past** | Found by watching, not reading. Measured at commit `5ebbc56` on the live installation: **62 of 85 enabled subscriptions had `next_due_at` more than an hour in the past**, one with `consecutive_304 = 2785`, and `app.sync_run` held **106,818 rows** for an installation two days old. Mechanism: `PlanNextDueAt` anchors on `LastSuccess`, and the 304 path passes `sub.LastSuccessAt` — which a 304 never updates, by design (§6.2 resets bookkeeping only on 200). So once a route has 304'd for longer than its own interval, every subsequent 304 recomputes THE SAME past instant, the claim query (ordered by `next_due_at`) finds it due on every 5-second tick, and HANGAR polls ESI continuously for data that has not changed. `esi_error_limit_remaining` sat at **97, not 100**, on an idle installation | **FIXED IN 20.5**, in `PlanNextDueAt` itself rather than at the three 304 call sites: a computed time that is not after `Now` is re-anchored on `Now`. The anchor's original intent — align to the upstream cache window when we polled early — is preserved for every case where it still means something. **It had to be fixed here because it made B30 unmeetable**: a subscription stuck an hour in the past always sorts ahead of one scheduled for the future and the claim is LIMITed, so the fifteen new fan-out subscriptions were created, were due, and could never be claimed. `internal/sync/duetime_spin_test.go`; `TestPlanNextDueAtAnchorsOnLastSuccess` amended, because it asserted a due time forty minutes in the past and its stated reasoning ("a failing subscription must not drift later") is right for a failing subscription and wrong for a 304 |
| **ESI returns the literal string `"None"` as the name of an unnamed asset** | Measured the first time the assets/names enrichment ran against real ESI: four singleton items, one genuinely named (`Ibis - CEODude`), three modules that came back `"None"` and were written to `app.asset.name` as if that were their name. Not an empty string, not an omitted element | **FIXED IN 20.5** before the phase closed. This is the failure mode the phase exists to refuse — not a blank, which looks wrong, but a plausible string that renders as a name and is not one. A player CAN name a container "None"; losing that one case is the right trade against putting a false name on every unnamed module in every hangar |
| **`GET /{characters,corporations}/{id}/wallet/{journal,transactions}` has returned 500 since Phase 15** | `internal/api/v1/owner_shared.go:118` passes the same `time.Time` to `BeforeDate` AND `BeforeJournalID`; `db/queries/wallet.sql` uses one named argument for a `timestamptz` comparison and a `bigint` one, so sqlc inferred `time.Time` for both. Live: `ERROR: invalid input syntax for type bigint: "9999-01-01 00:00:00 +0000 UTC" (SQLSTATE 22P02)`, with no cursor supplied at all. **Confirmed PRE-EXISTING** by building a binary from the pristine `5ebbc56` tree and reproducing it identically | **NOT FIXED — out of this phase's six items and recorded for 20.6.** The wallet screen has never worked on any installation. The fix is in the SQL, not the handler: the two comparisons need two arguments |
| **`GET /corporations/{corporation_id}/assets` is absent from `corporationDispatch` entirely** | Found while wiring the assets/names enrichment. The route is in the catalogue, `SyncAssets` is owner-generic, and no corporation's assets have ever synced on any installation | **FIXED IN 20.6 (B47)**, together with the corporation half of the assets/names enrichment as predicted. See the 20.6 table below for what wiring it exposed |
| **`internal/esi.Request` had no request body, so the gateway was structurally GET-only** | `POST /{owner}/{id}/assets/names` takes the item ids the assets list just returned. `http.NewRequestWithContext(ctx, req.Method, rawURL, nil)` — every call, always nil | **FIXED IN 20.5**, minimally. `Request.Body` is sent as `application/json`, and **a request carrying a body is never cached**: §5.3's key formula has no request-body term, so two POSTs to one path would collide on one key and the second would be served the first's answer. Adding a body hash to the key was REJECTED as a specification change made in passing; the conservative rule costs nothing here, because the one route that uses it carries no cache mode, no ETag and no Cache-Control from ESI |
| **The `$filter` measurement, before and after, on the same installation** | Baseline binary built from the pristine `5ebbc56` tree: `?standing=-10` gave **200 with the whole collection**, `?$filter=standing lt 0` gave **200 with the whole collection**. That is the third and worst of the three possible answers, and the caller cannot tell it happened — precisely the danger `APPENDIX_C_MIGRATION.md` §6.6 spells out for the /api/v2 shim and then only defends the shim against. After: **422** for both, with the field named | **The status is 422 and not 400, deliberately.** `00_SRS_v3.1.md`'s Phase 15 exit criterion states it outright ("adversarial query tests reject non-whitelisted filters with 422"); §6.6's 400 is a statement about the SHIM's `$filter`, which `internal/api/v2shim` already answers with exactly that. Two surfaces, two contracts. Neither is a 500 and neither is a silently-unfiltered result |
| **The L2 cache tier does not save REQUESTS, and "identical results" had to be defined before it could be asserted** | Establishing B34's gate condition established this: `esi.Client.Do` ALWAYS makes the conditional call — the validators come from `app.sync_subscription`, not from the cache — and the cache is consulted in exactly one place, to replay a stored body on a 304. So the upstream call count is identical in all four configurations, and the only tier-visible difference is whether a 304 carries a replayed body, which no caller reads (the worker's 304 branch never invokes a handler) | **No code change; recorded because it is why the outage answer is cheap.** The tier was never on the correctness path, which is exactly why "degrade to a miss" costs nothing and why a fall-through to Postgres would have been strictly worse than doing nothing |

### Findings made DURING 20.3, not by the original audit

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| **`rbac.PermissionsChangedHook` was nil in the process that performs RBAC mutations** | It is a package-level var. `cmd/hangar/work.go` set it; `serve.go` did not. 20.2 mounted the ENTIRE RBAC mutation surface — role create/delete, grant add/remove, user-role assign/revoke, and the squad endpoints — in the API server, so every one of those ran with the hook unset. Gate 2 trigger rows "RBAC role revoked" and "Squad membership removed" had a producer only in the process that performs none of them: revoking an admin role through the API recomputed `app.effective_permission`, wrote the §4.9 outbox row, and enqueued nothing | **Fixed in 20.3.** `cmd/hangar/revocation.go`'s `wireRevocationTriggers`, called by both roles |
| **`sso.Flow.OnOwnerHashChanged` was never set by any process** | The hook has existed since Phase 5 and `buildSSOFlow` never assigned it. Its own doc comment claimed "lifecycle.go wires the real invalidation regardless" — true of nothing, since `Lifecycle` had no production caller either. A character transferred to another EVE account kept every entitlement its previous owner earned, and logging in as the new owner did not disturb that | **Fixed in 20.3** |
| **Both refresh paths NOTIFIED an owner-hash change and then re-validated the token** | `RefreshCharacterToken` and `EnsureAccessToken` called `OnOwnerHashChanged` and carried straight on to `UpsertCharacterToken`, which sets `valid = true, invalid_reason = NULL`. §7.2 says "every stored token for that character must be invalidated **immediately**" — so on the one path where a transfer is observable, nothing was invalidated, and an out-of-band invalidation would have been resurrected two statements later | **Fixed in 20.3.** `Refresher.invalidateForOwnerChange`; the rotated token is deliberately not persisted, and `ErrOwnerHashChanged` tells the caller not to retry |
| **`esi_ledger_divergence` spikes to 40-55 under per-bucket concurrency — a THIRD instance of "operands describing different moments"** | Measured on the live installation immediately after 20.3's fix landed, by sampling `/metrics` every 3s for two minutes: `char-industry` 46, `char-social` 55, `char-detail` 51, `corp-industry` 50, `corp-member` 42, `corp-detail` 43, each persisting 3-6s. At steady state (~4.3 jobs/s, one character) the same 10-sample window reads max **1**. Mechanism: `local_remaining` is computed LIVE at scrape time while `server_remaining` is a snapshot from the last response that carried the header, so the gauge reports (requests settled since that reading) × 2 whenever several routes in the SAME rate-limit group for the SAME character settle between two reconciles. Not caused by 20.3 — `BackgroundRateLimitMax` returns 0 for every group but char-notification, so those buckets' behaviour is byte-for-byte unchanged | **FIXED IN 20.4.** Migration 00042 adds `app.esi_ledger_bucket.local_remaining_at_reading`; `RecordServerLedgerReading` writes both operands in one statement under the bucket lock the reconciler already holds, BEFORE the correction that makes them agree. `telemetry.DivergenceRow` no longer carries a live count at all — the field is deleted rather than left unread, so the wrong subtraction is unrepresentable. The live count stays on the ADMIN BOARD beside the paired one, because "how much headroom is there now" and "how far apart were we last time both were known" are two real operator questions. The freshness rule survives unchanged: a reconcile-time pair still goes stale on an idle bucket |
| **Gate 2 trigger row 6, "Corporation / alliance departure", has no producer** | `internal/sync` contains ZERO references to `internal/provisioning` (`grep -rln provisioning internal/sync` is empty). `handlers.SyncCharacterSheet` writes the new `app.character.corporation_id` and nothing recomputes entitlements, so a character leaving the corporation that granted their Discord roles keeps them until the next bulk reconcile. Not reachable through the RBAC hook either — the change is an ESI sync write, not an RBAC mutation | **FIXED IN 20.4.** `internal/sync/handlers.AffiliationChangedHook`, the package-level seam 20.3 specified, set in `cmd/hangar/revocation.go`'s `wireRevocationTriggers` so BOTH process roles carry it. `SyncCharacterSheet` reads the previous affiliation before the write and offers only a real move; a first sighting is not a departure, which matters because otherwise every character on every fresh installation would enqueue a revocation on its first sheet sync |
| **Gate 2 trigger row 8, "Admin platform lockdown", enqueues nothing** | `lockdownHandler` calls `SetPlatformLockdown` and returns. §2.3 marks the row "✓ must enqueue urgent" | **SETTLED IN 20.4, and the settlement turned up something larger.** The dilemma's premise was false: the freeze refused nothing, because NOTHING ANYWHERE READ `app.platform.locked_down` (see the 20.4 table below). With the freeze actually enforced, the question is real and is answered — LOCKING enqueues nothing (a freeze is "stop touching this platform", not "revoke everybody", and reading it as the latter would queue the removal of every group every member holds at the worst possible moment); UNLOCKING enqueues one full platform reconcile, because everything that changed during the freeze is owed the moment it lifts. Argued in full on `provisioning.Urgent.EnqueuePlatformReconcile` |
| **Gate 2 trigger row 9, "Strict Mode trip on any alt", is now transitively covered** | Strict Mode trips when any of a user's tokens becomes invalid, and every path that invalidates a token is rows 1–3, all of which now enqueue. `entitlement.GatherWorldState` re-reads live token state at evaluation time, so the recompute those rows trigger already sees the trip | **No action.** Stated because "covered transitively" and "not covered" look identical from a grep, and the next phase should not spend time re-deriving it |
| **`make bench-ledger-clustered` fails on this machine before any 20.3 change** | 8 consecutive runs at commit `88381d6` in a clean worktree: p99 = 10.13, 10.33, 10.89, 10.68, 11.16, 12.50, 10.27, 10.69 ms against a 10 ms budget. Throughput 6867 ops/s against a 2000 ops/s target | **RESOLVED IN 20.4 BY MEASUREMENT — the code was never the problem.** Five runs inside a `golang:1.26-alpine` container on the WSL2 Docker host, with Postgres reached over the Linux bridge instead of Windows port forwarding, on the same tree: p99 = **6.863, 7.151, 7.186, 7.322, 7.763 ms** (median 7.186) at 9,942–10,460 ops/s against a 2,000 ops/s target. The same tree, same commit, measured through Windows the same afternoon: 11.223 ms, failing three runs out of three. The ~3.3 ms gap is Docker Desktop for Windows port-forwarding tail latency, as suspected. No code was tuned and the 10 ms assertion was not relaxed |

### Findings made DURING 20.2, not by the original audit

The pattern of the last five phases held: running the system found what running the tests could
not.

| Finding | Evidence | Disposition |
| :-- | :-- | :-- |
| Every deliberate gateway refusal reached River as a FAILED JOB | Governor 1 exhaustion, a Governor 2 pause and both breakers all returned a plain error from `Client.Do`, which River records as a failure and retries on its own backoff — the exact inverse of §5.5's "the caller snoozes the subscription; it does not spin" | **Fixed in 20.2.** `internal/sync/worker/unavailable.go` |
| `PATCH /api/v1/admin/users/{id}` silently dropped `is_active` and `is_admin` | Both declared in `UpdateUserIn` since Phase 15, both advertised in the OpenAPI document, neither read by the handler. The endpoint answered 200 and changed nothing | **Fixed in 20.2** |
| Squad membership/role writes bypassed `internal/rbac` | The endpoints called the raw generated queries, so `app.effective_permission` was never recomputed and the §4.9 outbox row was never written. Adding somebody to a role-granting squad changed no permission they actually held | **Fixed in 20.2** |
| Two squad handlers discarded their errors | `_ = deps.Store.AddSquadRole(...)` reported success for a save that had failed — on a permission-granting operation | **Fixed in 20.2** |
| The ESI cache key omits the compatibility date | `cache.KeyInput` declares `CompatibilityDate` and `esi.Client.cacheKey` never populates it, so advancing the pin does not invalidate a single cached body. §5.3's formula names the field | **Fixed in 20.3.** `esi.Client.CompatibilityPin`, fed by the SAME `pinSource` closure the transport already uses for the `X-Compatibility-Date` header |
| **The char-notification reserve was stored, not applied per call** | §4.4's five-token reserve was passed as `AcquireRequest.MaxTokens`, and BOTH ledgers admit against the ceiling they have STORED — so the reduced value was written to `app.esi_ledger_bucket.max_tokens`, a row every caller shares. `esi_ledger_divergence` computes `max_tokens − consumed` against a server reporting against 15, so char-notification read a **permanent divergence of exactly 5** against Gate 1.3's tolerance of 1, on a healthy installation. Two callers with different ceilings for one bucket also thrashed the row, one real write per flip | **Fixed in 20.3.** `AcquireRequest.AdmissionMaxTokens`; `acquireLedgerEntrySQL` admits against `least(locked.max_tokens, $4)` |
| `/corporations/{id}/projects/{project_id}/contributors` is parsed with the wrong DTO | B38 renamed the path from `.../contributions` and kept the `contributions` parser. The spec's `CorporationsProjectsContributors` is an OBJECT of `{contributors, cursor}` whose elements are `{id, name, contributed}`; `ParseCorporationProjectContributions` expects a bare array of `{amount, character_id}`. It has never failed because the route is a fan-out path and therefore not subscribable, so it has never executed | **NOT fixed. Recorded for 20.5**, which owns B30 (route handlers that are never dispatched). Fixing it properly needs a decision about what `contributed` maps to in `app.corporation_project_contribution` |
